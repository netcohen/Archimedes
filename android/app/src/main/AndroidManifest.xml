<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".ArchimedesApp"
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Archimedes">

        <!-- Main status dashboard — Firestore real-time status listener -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Existing activities -->
        <activity android:name=".InboxActivity"       android:exported="false" />
        <activity android:name=".PairingActivity"     android:exported="false" />
        <activity android:name=".ApprovalActivity"    android:exported="false" />
        <activity android:name=".SecretInputActivity" android:exported="false" />
        <activity android:name=".CaptchaActivity"     android:exported="false" />

        <!-- Phase 31: New activities -->
        <activity android:name=".TaskActivity"     android:exported="false" />
        <activity android:name=".SettingsActivity" android:exported="false" />

        <!-- Phase 31: FCM service — receives push notifications from Core via Net -->
        <service
            android:name=".ArchimedesFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>
</manifest>
